# Godot 4 web builds are exported with thread support, which needs
# SharedArrayBuffer, which the browser only hands out to a cross-origin-isolated
# document. Without these two headers the engine aborts at startup.
header Cross-Origin-Opener-Policy "same-origin"
header Cross-Origin-Embedder-Policy "require-corp"
